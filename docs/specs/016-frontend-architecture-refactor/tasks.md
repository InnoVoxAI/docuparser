---

description: "Task list for feature implementation"

---

# Tasks: Refatoração Arquitetural do Frontend DocuParse (alinhamento a `frontend_rules.md`)

**Input**: Design documents from `docs/specs/016-frontend-architecture-refactor/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/module-boundaries.md](./contracts/module-boundaries.md), [quickstart.md](./quickstart.md)

**Tests**: A suíte de testes automatizados já existe (`docuparse-project/frontend/src/__tests__`, 679 linhas) e é tratada como piso de regressão obrigatório (FR-010) — as tarefas abaixo incluem reforço/expansão de testes onde a spec exige (US1, FR-010, SC-006), mas não geram um ciclo TDD completo por tarefa de refatoração pura (mover código sem mudar comportamento).

**Organization**: Tarefas agrupadas por user story (`spec.md`), na ordem de prioridade P1 → P1 → P2 → P3. Como esta é uma migração estrutural (não CRUD), a User Story 2 concentra o grosso do trabalho técnico (extração módulo a módulo); User Stories 1, 3 e 4 têm tarefas próprias e verificáveis independentemente, mas também são reforçadas por checkpoints embutidos na User Story 2 (referenciados explicitamente).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Pode rodar em paralelo (arquivos diferentes, sem dependência de tarefa incompleta)
- **[Story]**: User story à qual a tarefa pertence (US1–US4)
- Caminhos de arquivo sempre relativos a `docuparse-project/frontend/`

## Path Conventions

Projeto único já existente: `docuparse-project/frontend/src/`. Nenhum novo
projeto/pacote é criado; a estrutura-alvo (`src/app`, `src/modules/*`,
`src/shared/*`) é criada dentro do pacote atual (ver `plan.md` → Project
Structure).

---

## Phase 1: Setup

**Purpose**: Preparar dependências e baseline antes de qualquer mudança estrutural.

- [X] T001 Rodar o gate completo sobre o estado atual (`npm run typecheck && npm run test:run && npm run build` em `docuparse-project/frontend`) e registrar como baseline "verde" pré-migração
  - **Resultado (2026-07-24)**: `typecheck` PASS · `build` PASS · `test:run` **NÃO** verde — 27/28 passam, 1 falha pré-existente e reproduzível de forma consistente (3/3 execuções), não causada por nenhuma mudança desta feature (nenhum código-fonte foi tocado antes deste gate, apenas `npm install`).
    - Teste: `src/__tests__/flows.test.tsx > Upload > envia um documento manual e mostra a confirmação`.
    - Causa raiz identificada por depuração isolada (script standalone fora da suíte): o `axios.post` com corpo `FormData` contra uma instância com `baseURL` relativo, interceptado pelo MSW (`XMLHttpRequestInterceptor`) sob jsdom + Node 22, **nunca resolve nem rejeita** — trava indefinidamente (reproduzido também fora do componente `UploadView`, isolando `axios`+`FormData`+MSW+jsdom como a combinação problemática, não a lógica de `UploadView`). Não é flakiness de timing; é um hang determinístico.
    - **Decisão (usuário, 2026-07-24)**: investigação/correção adiada para depois — apenas documentar. Baseline pré-migração aceito como 27/28 verde, com esta falha conhecida registrada como débito pré-existente. Ao rodar o "gate completo" em qualquer etapa futura da Fase 4+ (T018, T020, T025, T032, T035, T039, T041, T042, T046) e na Fase 5/7, este teste específico deve continuar sendo a única falha esperada — qualquer falha adicional é regressão real introduzida pela migração. Se ao mexer em `modules/upload` (T042) o comportamento de upload for tocado, esta falha deve ser revisitada/corrigida naquele ponto, não ignorada.
- [X] T002 [P] Adicionar dependências de produção ao `docuparse-project/frontend/package.json`: `react-router`, `@tanstack/react-query`, `@tanstack/react-query-devtools`, `zustand`, `react-hook-form`, `zod`, `@hookform/resolvers`
- [X] T003 [P] Adicionar dependências de desenvolvimento ao `docuparse-project/frontend/package.json`: `eslint`, `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin`, `eslint-plugin-react-hooks`, `eslint-plugin-jsx-a11y`, `eslint-plugin-boundaries`, `prettier`, `vitest-axe`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Rede de segurança (lint/testes) e esqueleto da estrutura-alvo — bloqueia todas as user stories.

**⚠️ CRITICAL**: Nenhuma extração de módulo (US2) pode começar antes desta fase.

- [X] T004 Criar `docuparse-project/frontend/eslint.config.js` com `@typescript-eslint` (recomendado + `no-explicit-any` como error), `react-hooks/rules-of-hooks`, `react-hooks/exhaustive-deps`, `jsx-a11y` recomendado, e regra `max-lines` (150, override para arquivos de dados/tipos) — ver `research.md` §7
  - **Resultado (2026-07-24)**: Config plano ESLint 9 criado. `react-hooks` restrito às 2 regras nomeadas pela tarefa (`rules-of-hooks`/`exhaustive-deps`), não ao preset `recommended` completo (v7.1.1 do plugin inclui regras experimentais adicionais fora do escopo pedido). Override de `max-lines` estendido a arquivos de teste (`**/__tests__/**`, `**/*.test.{ts,tsx}`) além de dados/tipos — FR-012/SC-002 limitam apenas "arquivo de componente", não specs. `npm run lint` sobre o `src/` atual (monólito ainda intacto) reporta 33 erros/11 avisos pré-existentes em `main.tsx`/`types.ts` (16 `no-explicit-any`, 11 `exhaustive-deps` warn, 14 `jsx-a11y`, 1 `no-unused-vars`, 1 `no-empty`, 1 `max-lines` no próprio `main.tsx`) — nenhum é regressão desta tarefa; são débito pré-existente a resolver durante a extração módulo a módulo (Fase 4) e o endurecimento de lint (Fase 5/T047, escopado a "código migrado"). A regra de fronteira de módulo (`eslint-plugin-boundaries`) fica para T047 (Fase 5), conforme o próprio enunciado desta tarefa não a menciona.
- [X] T005 [P] Criar `docuparse-project/frontend/.prettierrc` e adicionar scripts `lint`/`format` em `docuparse-project/frontend/package.json`
  - **Resultado (2026-07-24)**: `.prettierrc` alinhado ao estilo já usado no código (sem `;`, aspas simples, indentação 4, trailing commas). Scripts `lint` (`eslint .`) e `format` (`prettier --write .`) adicionados. `npm run format` **não** foi executado sobre o repo inteiro (reformataria `main.tsx` em massa, fora do escopo desta tarefa) — apenas validado com `--check` sobre os arquivos novos desta fase.
- [X] T006 [P] Configurar threshold de cobertura ≥80% por diretório de módulo em `docuparse-project/frontend/vitest.config.ts` (aplicado progressivamente conforme módulos forem extraídos — ver `data-model.md` "Cobertura de teste")
  - **Resultado (2026-07-24)**: Adicionada entrada `'src/modules/**'` (80% linhas/statements/branches/functions) ao mapa `thresholds` do provider v8 (suporta glob por diretório nativamente), mantendo os limiares globais existentes (piso de regressão do monólito) intocados.
- [X] T007 Criar esqueleto de pastas vazio: `docuparse-project/frontend/src/app/`, `docuparse-project/frontend/src/modules/`, `docuparse-project/frontend/src/shared/{components,hooks,lib,types,utils}` (ver `plan.md` → Project Structure)
- [X] T008 Mover as 3 instâncias axios (`api`, `authApi`, `comApi`) e o interceptor JWT de `docuparse-project/frontend/src/main.tsx` para `docuparse-project/frontend/src/shared/lib/http.ts`; atualizar `main.tsx` para importar de lá
  - **Resultado (2026-07-24)**: Interceptor JWT movido de um `useEffect` de attach/eject dentro de `AuthProvider` para o escopo do módulo em `http.ts` (anexado uma única vez à criação das instâncias). Comportamento idêntico preservado: `attachToken` já lia o token do `localStorage` a cada requisição, sem depender de estado React, e `AuthProvider` só monta uma vez por sessão do app — logo o attach/eject por ciclo de vida era supérfluo. `tsc --noEmit` limpo após a mudança.
  - **Correção retroativa (2026-07-24, achada durante T019)**: apesar do resultado acima, `main.tsx` **nunca chegou a importar** `api`/`authApi`/`comApi` de `shared/lib/http.ts` — continuou definindo as 3 instâncias localmente (com `CORE`/`COM` de `import.meta.env`, que `http.ts` não tinha) até a Fase 4/T019-T020 as substituir de fato. `http.ts` também não incluía `adminApi`. Isso só foi descoberto e corrigido em T019/T020 porque mover `AuthProvider` para fora de `main.tsx` (T019) removeu o único código que reanexava o interceptor às instâncias *locais* de `main.tsx` — o que teria quebrado silenciosamente o header `Authorization` de toda tela ainda não migrada. Se você encontrar outra nota "[X] Resultado" neste arquivo que pareça implausível, confira o código antes de confiar nela — histórico de tasks.md pode divergir do estado real.
- [X] T009 [P] Criar `docuparse-project/frontend/src/shared/lib/queryClient.ts` (instância `QueryClient`, ainda não consumida por nenhum componente)
- [X] T010 Upgrade React 18→19 em `docuparse-project/frontend/package.json` (+ `@types/react`, `@types/react-dom`) — commit isolado; rodar gate completo (T001) antes de prosseguir
  - **Resultado (2026-07-24)**: `react`/`react-dom` 18→19.2.8, `@types/react`/`@types/react-dom` 18→19. Instalado com `--legacy-peer-deps` (peer `react-dom@^18` de `@testing-library/react@14` é aviso esperado e aceito — ver research.md §10: v14 é compatível com React 19 na prática). Gate completo (T001: typecheck+test:run+build) verde — 27/28 testes, mesma falha pré-existente documentada em T001, nenhuma regressão nova.
- [X] T011 Migrar Tailwind v3→v4: `docuparse-project/frontend/src/index.css` (`@import "tailwindcss"`), `docuparse-project/frontend/tailwind.config.js`→`.ts`, `docuparse-project/frontend/postcss.config.cjs` — commit isolado; rodar gate completo + checklist visual (`quickstart.md`) antes de prosseguir
  - **Resultado (2026-07-24)**: `tailwindcss`+`@tailwindcss/postcss` 4.3.3 instalados; `postcss.config.cjs` usa só o plugin `@tailwindcss/postcss` (autoprefixing interno ao v4); `index.css` usa `@import "tailwindcss"` + `@config "../tailwind.config.ts"` (mantém `content`/`theme.extend` explícitos, sem uso de sintaxe descontinuada — confirma research.md §9); `tailwind.config.js`→`.ts` (tipado via `satisfies Config`), adicionado a `tsconfig.node.json` para cobertura de typecheck do editor. **Efeito colateral encontrado e corrigido**: a reinstalação de dependências (React 19 + Tailwind v4, ambas via `--legacy-peer-deps`) des-hoisteou `@testing-library/dom` (antes no root `node_modules/`, virou aninhado só sob `@testing-library/react`), quebrando `@testing-library/user-event` (`ERR_MODULE_NOT_FOUND`) em 4 arquivos de teste. Corrigido declarando `@testing-library/dom` como devDependency explícita (`^9.3.4`, mesma versão já resolvida) — prática padrão para projetos que usam `user-event` diretamente. Gate completo verde após a correção: typecheck limpo, build OK (CSS de saída validado — classes `.primary-button`/`.success-button`/`.danger-button` compilam com os mesmos valores visuais via variáveis CSS do v4), 27/28 testes (mesma falha pré-existente, sem regressão). **Checklist visual manual (`quickstart.md`) não foi percorrido em navegador** — sem ferramenta de screenshot/browser disponível neste ambiente; validação limitada à inspeção estrutural do CSS gerado. Recomenda-se passe visual manual pelo usuário antes do commit desta etapa.

**Checkpoint**: Fundação pronta — rede de segurança ativa, esqueleto de pastas criado, dependências novas instaladas, upgrades de React/Tailwind validados isoladamente.

---

## Phase 3: User Story 1 - Preservação total do comportamento durante toda a transição (Priority: P1) 🎯 MVP

**Goal**: Garantir que a suíte de testes automatizados e o checklist de regressão manual cubram hoje todas as telas, servindo de oráculo confiável para todas as etapas seguintes.

**Independent Test**: Rodar `npm run test:run` e confirmar que cada uma das 10 telas (Login, Dashboard, Inbox, Validação, Aprovados, Rejeitados, Operações, Configurações, Usuários, Roles) tem pelo menos um teste de fumaça passando; percorrer manualmente o checklist de `quickstart.md` sobre o estado atual.

### Implementation for User Story 1

- [X] T012 [US1] Auditar `docuparse-project/frontend/src/__tests__/screens.test.tsx` e demais specs em `docuparse-project/frontend/src/__tests__/` e listar quais das 10 telas não têm teste de fumaça próprio
  - **Resultado (2026-07-24)**: 8/10 telas já tinham cobertura de fumaça dedicada (Login em `auth.test.tsx`; Inbox, Dashboard, Operações, Configurações, Usuários, Roles em `flows.test.tsx`; Validação em `validation.test.tsx`, 8 testes). **2/10 sem nenhum teste**: Aprovados (`ApprovedView`) e Rejeitados (`RejectedView`, a listagem completa — distinta do modal "Documento Rejeitado" já testado em `flows.test.tsx`). Achado adicional relevante para US1 ("preservação total do comportamento"): nenhuma das duas é alcançável por navegação real hoje — `NAV_ITEMS` (main.tsx:138) não inclui `approved`/`rejected` e nenhum `setActiveView(...)` no código aponta para esses valores; são views mortas do ponto de vista de navegação, embora renderizáveis diretamente (mesmo padrão já usado para `ValidationView`).
- [X] T013 [US1] Adicionar os testes de fumaça faltantes identificados em T012, um arquivo por tela em `docuparse-project/frontend/src/__tests__/`
  - **Resultado (2026-07-24)**: Exportadas `ApprovedView`/`RejectedView` de `main.tsx` (eram locais; mesmo tratamento já dado a `ValidationView`) e criados `approved.test.tsx` (2 testes: lista + estado vazio) e `rejected.test.tsx` (2 testes: lista com motivo/toggle + estado vazio), renderizando os componentes diretamente via MSW — não pela árvore de navegação, já que não é alcançável (ver T012). Gate completo verde: `typecheck` limpo, `lint` sem novos problemas (apenas o débito pré-existente já documentado em T004), `test:run` 32/32 (8 arquivos), `build` OK. Observação: a falha de hang documentada em T001 (`Upload > envia um documento manual...`) **não reproduziu** nesta execução nem em reexecução isolada — sinal de que pode ter sido corrigida como efeito colateral dos upgrades da Fase 2 (React 19/Tailwind v4/deps), não investigado a fundo por estar fora do escopo desta tarefa.
- [X] T014 [US1] Revisar e ajustar o checklist de regressão manual em `docs/specs/016-frontend-architecture-refactor/quickstart.md` com qualquer lacuna encontrada em T012/T013
  - **Resultado (2026-07-24)**: Adicionada nota ao checklist visual manual explicando que "Aprovados" e "Rejeitados" (listagem completa) não são alcançáveis manualmente hoje pelos motivos levantados em T012, e que sua única cobertura no momento é automatizada (`approved.test.tsx`/`rejected.test.tsx`) — evita que um executor do checklist gaste tempo procurando um link de navegação que não existe.
- [X] T015 [US1] Executar o checklist de regressão manual completo (`quickstart.md`) sobre o estado atual (baseline pré-extração) e registrar o resultado como referência de comparação para as próximas fases
  - **Resultado (2026-07-24)**: Executado com Playwright + Chromium headless (instalados ad-hoc para esta verificação, não commitados) contra os 4 backends reais (`start_backend.sh`) e `npm run dev`, logado como `admin@docuparse.com` (seed). **PASS, sem erros de console**: Login, Logout, Permissões por item de menu (admin vê os 8 itens), Dashboard (métricas + listagem), Inbox (busca filtra corretamente), Operações/DLQ (summary renderiza, 0 eventos), Configurações (5 abas: Email/WhatsApp/OCR/Extração/Integrações, todas renderizam), Usuários (lista com o admin seed), Roles (lista com permissões). **Upload**: a UI confirma corretamente ("Documento recebido: `<id>`") — porém o pipeline downstream (OCR/extração automática) não pôde ser exercitado de ponta a ponta neste sandbox por três problemas de infraestrutura pré-existentes e não relacionados a esta feature de frontend, descobertos e contornados nesta sessão: (1) `backend-com/config.py` calcula `PROJECT_DIR` como `/` (bug de índice em `.parents[3]`), fazendo o storage local default tentar escrever em `/.docuparse-storage` (permissão negada) — contornado via `DOCUPARSE_LOCAL_STORAGE_DIR` explícito; (2) Redis (exigido por `DOCUPARSE_EVENT_BUS=redis`) não estava instalado/rodando neste ambiente — contornado instalando `redis-server` + client Python ad-hoc; (3) mesmo com os dois acima corrigidos, `POST /api/ocr/events/document-received` falha com `ProgrammingError: relation "documents_documentevent" does not exist` — a tabela do model `DocumentEvent` (migração `0001_initial`, marcada `[X]` aplicada) não existe de fato no Postgres deste sandbox; não investigado a fundo por ser claramente um problema de estado do banco deste ambiente efêmero, fora do escopo de "só Fase 3". **Consequência prática**: Validação e o modal "Documento Rejeitado" (Rejeitados) não puderam ser exercitados manualmente ponta a ponta nesta sessão (dependem de um documento chegar a `EXTRACTION_COMPLETED`/`REJECTED`); permanecem cobertos apenas pela suíte automatizada (`validation.test.tsx` 8 testes, `flows.test.tsx`). Nenhuma regressão de comportamento do frontend foi observada em nada do que pôde ser exercitado — os 3 problemas acima são todos do lado do backend/infra, préexistentes, e não foram introduzidos por esta feature. Recomenda-se investigar a tabela `documents_documentevent` ausente como um item separado (fora do escopo desta feature de frontend). Serviços de teste (dev server, 4 backends, Redis ad-hoc) foram encerrados ao final desta tarefa; nada foi deixado rodando.

**Checkpoint**: User Story 1 entregável isoladamente — rede de regressão reforçada, útil mesmo sem nenhuma extração estrutural ainda ter começado.

---

## Phase 4: User Story 2 - Arquitetura-alvo disponível e verificável (Priority: P1)

**Goal**: Extrair o monólito `src/main.tsx` para `src/app` + `src/modules/*` + `src/shared/*`, com roteamento real (React Router v7), camada de consulta centralizada (TanStack Query), formulários declarativos (RHF+Zod) e, apenas onde sobrar, estado de cliente em Zustand.

**Independent Test**: Inspecionar `docuparse-project/frontend/src/` e confirmar: nenhum arquivo concentra múltiplas telas não relacionadas; navegação por rotas reais; busca de dados via camada de consulta centralizada; import direto de arquivo interno de outro módulo é sinalizado pelo lint.

### 4a. `shared/components` (primitivas de UI — extração pura, zero risco)

- [X] T016 [P] [US2] Extrair `Alert`, `EmptyState`, `Field`, `Metric` de `docuparse-project/frontend/src/main.tsx` para `docuparse-project/frontend/src/shared/components/`
  - **Resultado (2026-07-24)**: 4 arquivos criados (um componente por arquivo, `ReactNode` importado de `react` em vez de `React.ReactNode`, único ajuste sobre a extração pura).
- [X] T017 [P] [US2] Extrair `SearchInput`, `StatusBadge`, `KeyValueGrid`, `Pagination`, `ConfirmDialog` de `docuparse-project/frontend/src/main.tsx` para `docuparse-project/frontend/src/shared/components/`
  - **Resultado (2026-07-24)**: `STATUS_LABELS` (usado só por `StatusBadge`) colocado junto no mesmo arquivo, não exportado. `Pagination` leva consigo os ícones `ChevronLeft`/`ChevronRight` (removidos do import de `main.tsx`, não usados em mais nenhum lugar).
- [X] T018 [US2] Atualizar `docuparse-project/frontend/src/main.tsx` para importar essas primitivas de `docuparse-project/frontend/src/shared/components` (remover definições locais); rodar gate completo (T001) + checklist visual (`quickstart.md`); commit da etapa
  - **Resultado (2026-07-24)**: Gate completo verde — `typecheck` limpo, `test:run` **32/32** (a falha de hang documentada em T001 permanece ausente, confirmando a suspeita de T013 de que foi resolvida como efeito colateral da Fase 2), `build` OK. `lint`: 1 erro (`max-lines` em `main.tsx`, esperado até a remoção final do monólito em T046) + 11 avisos `exhaustive-deps` pré-existentes documentados em T004 — nenhum problema novo. Checklist visual manual (`quickstart.md`) **não** executado em navegador nesta etapa (sem ferramenta de screenshot/browser neste ambiente, mesma limitação de T011); mudança é extração pura sem alteração de JSX/classes, risco visual mínimo.

### 4b. `modules/auth`

- [X] T019 [US2] Criar `docuparse-project/frontend/src/modules/auth/{context.tsx,components/LoginPage.tsx,components/PermissionGuard.tsx,types.ts,index.ts}` movendo `AuthContext`/`AuthProvider`/`useAuth`/`LoginPage`/`PermissionGuard`/`AcessoNaoAutorizado` de `src/main.tsx`
  - **Resultado (2026-07-24)**: `AcessoNaoAutorizado` em arquivo próprio (`components/AcessoNaoAutorizado.tsx`), não em `PermissionGuard.tsx` (mapeamento 1:1 arquivo↔componente, consistente com T016/T017). `LoginPage` (226 linhas originais) dividido em `LoginPage.tsx` (contêiner com estado/handlers) + `LoginForm.tsx`/`RegisterForm.tsx` (JSX puro), cada um ≤150 linhas (FR-012/SC-002) — extração pura, nenhuma lógica de validação alterada (isso é T044). `AuthProvider` deixou de reanexar o interceptor JWT a cada montagem (useEffect removido): as instâncias `api`/`comApi`/`adminApi` já vêm com o interceptor anexado uma única vez em `shared/lib/http.ts` (mesma conclusão já registrada em T008). Como consequência necessária (não opcional — sem isso toda chamada autenticada do monólito ainda não migrado perderia o header `Authorization`), completei o que T008 deveria ter feito: `shared/lib/http.ts` ganhou `adminApi` + suporte a `VITE_BACKEND_CORE_URL`/`VITE_BACKEND_COM_URL` (paridade com o `main.tsx` original), e `main.tsx` passou a importar `api`/`comApi`/`adminApi` de lá em vez de instâncias locais duplicadas. Também movi `asApiError`/`readError` (`shared/utils/errorMessages.ts`) e `formatDate` (`shared/utils/formatDate.ts`) para `shared/utils` como pré-requisito do `LoginPage` (adiantamento parcial de T045, necessário agora pois nenhum consumidor de módulo pode importar de `main.tsx`).
- [X] T020 [US2] Atualizar `docuparse-project/frontend/src/main.tsx` para consumir `modules/auth` apenas pelo barrel `index.ts` (ver `contracts/module-boundaries.md`); rodar gate completo + checklist visual; commit da etapa
  - **Resultado (2026-07-24)**: `main.tsx` reexporta `AuthProvider` (consumido por `src/__tests__/utils.tsx`). Gate completo verde: `typecheck` limpo, `test:run` 33/33, `build` OK; `lint` só com o `max-lines` esperado em `main.tsx` (5218 linhas, decrescente) + os 11 avisos `exhaustive-deps` pré-existentes — nenhum problema novo. Checklist visual manual não executado em navegador (mesma limitação já registrada em T018/T011); risco visual mínimo (extração pura, nenhuma classe/JSX alterada fora da divisão de `LoginPage`, que preserva o markup original).
- [X] T021 [P] [US2] Adicionar teste de acessibilidade (`vitest-axe`, ver `quickstart.md`) para `LoginPage` em `docuparse-project/frontend/src/modules/auth/__tests__/LoginPage.a11y.test.tsx` (SC-006)
  - **Resultado (2026-07-24)**: `expect.extend` do matcher `toHaveNoViolations` centralizado em `src/__tests__/setup.ts` (evita repetição em cada teste de a11y futuro, T033/T040). Achado de compatibilidade: `vitest-axe@0.1.0` declara seus tipos via `namespace Vi` (mecanismo de matcher customizado do Vitest 2+), incompatível com o Vitest `^1.4.0` já instalado neste projeto — `tsc` não reconhecia `toHaveNoViolations` mesmo com o matcher registrado em runtime. Corrigido com uma augmentation local (`src/__tests__/vitest-axe.d.ts`) usando o padrão oficial do Vitest 1.x (`declare module 'vitest' { interface Assertion extends AxeMatchers {} }`). Teste passa (0 violações em `LoginPage`); gate completo verde (33/33 testes).

### 4c. Roteamento (React Router v7)

- [X] T022 [US2] Criar `docuparse-project/frontend/src/app/router.tsx` com `createBrowserRouter` (modo data router), uma rota por item de `NAV_ITEMS` atual, guarda de permissão via `useAuth().hasPermission` (ver `research.md` §3, `data-model.md` "Rota")
  - **Resultado (2026-07-24)**: Seguida a abordagem já esboçada na nota de handoff acima. `App()` (main.tsx:625-890) virou `AppLayout` (exportado) — mesma lógica de estado/handlers (`schemas`/`layouts`/`selectedDocumentId`/`selectedDocument`/`refreshSignal`/`rejectedModal`/`refreshData`/`handleReprocessDocument`/`handleDeleteDocument`) preservada 1:1, agora provida a`` `<Outlet context={... satisfies AppOutletContext} />` `` em vez do switch de `activeView`. `router.tsx` define um componente fino por rota (`DashboardRoute`, `InboxRoute`, etc.) que faz `useOutletContext<AppOutletContext>()` + `<PermissionGuard code="..." fallback={<AcessoNaoAutorizado/>}>` — transformação quase 1:1 de cada bloco antigo `{activeView === 'x' ? (...) : null}`. Rotas: `/dashboard`, `/inbox`, `/upload`, `/validation`, `/operations`, `/settings`, `/users`, `/roles`, `/tenants` (mapeamento 1:1 com `NAV_ITEMS` via helper `navPath(id) = \`/${id}\``, exportado de `main.tsx`); `/` redireciona (`IndexRedirect`) para o primeiro item permitido, mesma regra do antigo estado inicial de `activeView`; `*` redireciona para `/`. `navigateToValidation`/`onNavigateToUpload`/`onBackToInbox` passaram a usar `useNavigate()` em vez de `setActiveView`. `approved`/`rejected` (achados mortos do T012) **não** viraram rota — continuam só renderizáveis diretamente, como já documentado. **Achado de arquitetura de teste**: `createBrowserRouter` liga-se ao `window.location`/`history` reais; como o `jsdom` da suíte persiste entre `it()`s do mesmo arquivo, um router-singleton faria um teste herdar a URL deixada pelo teste anterior. Resolvido exportando `createAppRouter()` (fábrica) além do singleton `router` (usado em produção); `Root` (ver T024) aceita um `router` opcional para injeção; `src/__tests__/utils.tsx` reseta `window.history` e cria um router novo a cada `renderApp()`. Sem essa correção, a suíte passava (33/33) mas o Vitest reportava 2 "Unhandled Errors" (`DataCloneError` ao tentar serializar um `AxiosError` de uma requisição MSW não mockada disparada por uma rota herdada de outro teste) — confirmado como regressão real comparando com o estado pré-T022 via `git stash` (0 erros na baseline) antes de corrigir; após a correção, 0 erros em 3 execuções consecutivas.
- [X] T023 [US2] Criar `docuparse-project/frontend/src/shared/components/ErrorBoundary.tsx` (classe, `componentDidCatch`, fallback amigável) e associar como `errorElement` de cada rota de módulo em `router.tsx` (FR-007)
  - **Resultado (2026-07-24)**: Classe simples (`getDerivedStateFromError`/`componentDidCatch` com `console.error`, fallback `<Alert tone="error">`); `children` opcional (o React Router usa este componente como `errorElement`, sem lhe passar filhos — ele não é montado como boundary tradicional em torno de conteúdo próprio nesse uso, é o próprio conteúdo de fallback exibido pelo mecanismo interno de erro do data router). Exportado por `shared/components/index.ts`; associado como `errorElement` tanto na rota de layout (`/`) quanto em cada rota-folha individual em `router.tsx`.
- [X] T024 [US2] Substituir a alternância `activeView`/switch de `App` em `src/main.tsx` pelo router; criar `docuparse-project/frontend/src/app/main.tsx` (bootstrap com `RouterProvider` + `QueryClientProvider`)
  - **Resultado (2026-07-24)**: `src/app/main.tsx` criado como novo bootstrap (`ReactDOM.createRoot` + `QueryClientProvider` + `AuthProvider` + `Root`); `Root` (movido de `main.tsx`) decide `loading`/`LoginPage`/`RouterProvider` exatamente como antes, com um `router` opcional para injeção em teste. `index.html` atualizado (`<script src="/src/app/main.tsx">`) — sem isso o novo bootstrap nunca rodaria (conflito de entrypoint identificado na nota de handoff). `src/main.tsx` **não foi removido** (isso é T046): continua exportando os componentes ainda não extraídos (`Dashboard`, `InboxView`, `UploadView`, `OperationsView`, `SettingsView`, `GerenciarUsuarios`, `GerenciarRoles`, `TenantsView`, além de `ApprovedView`/`RejectedView`/`ValidationView` já exportados desde a Fase 3/T013), consumidos por `router.tsx` até T026-042 os extraírem para `modules/*`. `AuthProvider`/`LoginPage`/`Root`/bootstrap saíram de `main.tsx` (agora vivem em `modules/auth`/`app/main.tsx`).
- [X] T025 [US2] Ajustar `docuparse-project/frontend/src/__tests__/screens.test.tsx` apenas nos seletores de navegação (URLs), sem alterar labels/permissões testados; rodar gate completo + checklist visual; commit da etapa
  - **Resultado (2026-07-24)**: `src/__tests__/utils.tsx` (`renderApp`) ajustado para a nova árvore (`QueryClientProvider` + `AuthProvider` + `Root`, importados de `../app/main`/`../modules/auth`) e para resetar `window.history`/criar router novo por render (ver achado em T022). Teste original de `screens.test.tsx` (labels presentes) mantido sem alteração de asserções — só herdou o novo `renderApp()`. Seguindo a sugestão 5 da nota de handoff, foi adicionado um teste novo (`navega por rotas reais ao clicar nos itens de menu`) que clica em "Dashboard"/"Inbox" e afirma `window.location.pathname`, validando de fato FR-002 (rotas reais) em vez de só labels — endpoints escolhidos por já terem handler MSW padrão em `mocks/handlers.ts` (endpoints de DLQ/roles não têm handler padrão; usá-los reproduziu o mesmo `DataCloneError` de MSW-passthrough não mockado, então o teste foi ajustado para não depender de mocks adicionais). **Gate completo**: `typecheck` limpo, `lint` só com o `max-lines` pré-existente em `main.tsx` (5140 linhas, decrescente) + os 11 avisos `exhaustive-deps` já documentados em T004 — nenhum problema novo; `test:run` **34/34** (3 execuções consecutivas, 0 "Unhandled Errors"); `build` OK (bundle único ainda sem code-splitting — aviso de chunk >500kB pré-existente, fora do escopo desta tarefa). **Checklist visual (`quickstart.md`)**: sem backends/browser completos neste ambiente (mesma limitação de T011/T018/T020), mas desta vez a mudança é comportamental (rotas reais), não só estrutural — então, adicionalmente ao gate automatizado, rodou-se uma verificação funcional ad-hoc com Playwright headless (instalado ad-hoc, não commitado, mesmo padrão do T015) contra `npm run dev` com as chamadas de API interceptadas/mockadas (sem subir os 4 backends): confirmado sem erros de console — LoginPage para usuário não autenticado em `/`; após autenticar, `/` redireciona para o primeiro item permitido (`/upload`); clicar em cada item do menu (Dashboard/Operações/Configurações/Usuários/Roles/Upload/Inbox) navega para a URL real correspondente; **reload físico** (não só navegação client-side) em `/operations` resolve corretamente a rota (prova de que é roteamento real via URL, não só estado de componente); caminho desconhecido redireciona para `/`.

### 4d. `modules/documents` (+ TanStack Query)

- [X] T026 [US2] Criar `docuparse-project/frontend/src/modules/documents/hooks/queryKeys.ts` com `documentKeys` (ver `data-model.md`)
  - **Resultado (2026-07-24)**: Fábrica criada exatamente como especificada em `data-model.md` (`all/list/count/detail/fieldVersions`).
- [X] T027 [US2] Converter `useDocumentPage` (hoje em `src/main.tsx`) em `useDocumentsQuery` baseado em `useQuery` em `docuparse-project/frontend/src/modules/documents/hooks/useDocumentsQuery.ts`, preservando a assinatura de retorno (`page,setPage,search,setSearch,data,loading,error,refresh`) consumida pelas telas
  - **Resultado (2026-07-24)**: Assinatura de retorno preservada 1:1. `page`/`search` continuam `useState` locais (estado de UI, não pertencem ao cache do servidor); `data`/`loading`/`error` vêm de `useQuery` (`placeholderData: keepPreviousData` evita "flash" vazio ao trocar de página, igual ao comportamento antigo de não limpar `data` durante um refetch). O polling automático (`autoRefresh`) virou `refetchInterval` como função (verifica `RECEIVED`/`OCR_COMPLETED` nos resultados já carregados, senão desliga). **Decisão de infraestrutura**: `shared/lib/queryClient.ts` ganhou `defaultOptions.queries.retry: false` — o hook original nunca re-tentava uma requisição falha (erro exibido imediatamente); sem essa opção, o retry padrão do TanStack Query (3x com backoff) atrasaria a exibição de erro em segundos, uma regressão de UX perceptível. `useDocumentPage` **não foi removido** de `main.tsx`: `ReferenceDocumentPanel` (Configurações, ainda não extraída — Fase 4f/T036-040) continua a consumir; apenas `fetchDocumentCount` (função irmã, só usada pelo `Dashboard` antigo) foi removida por ficar morta. `useDocumentPage` ganhou um comentário explicando por que continua ali.
- [X] T028 [P] [US2] Converter `fetchDocumentCount` em `useQuery` própria (chave `documentKeys.count`) em `docuparse-project/frontend/src/modules/documents/hooks/useDocumentCount.ts`
  - **Resultado (2026-07-24)**: Refaz a contagem quando `refreshSignal` muda (upload/reprocessar/excluir/"Atualizar"), preservando o gatilho de refresh externo. **Nuance não preservada, documentada no código**: o `Dashboard` original também recontava métricas a cada tick do polling automático da listagem (dependência `data` no efeito antigo, não só `refreshSignal`) — efeito colateral de uma implementação, não um requisito testado; manter essa cadeia exigiria expor `dataUpdatedAt` do `useDocumentsQuery` e compor uma chave sintética, complexidade desproporcional para uma métrica já documentada como "best-effort" no código original. Nenhum teste depende deste comportamento.
- [X] T029 [US2] Converter as mutações (`handleReprocessDocument`, `handleDeleteDocument`, validação/aprovação/rejeição) em `useMutation` com `invalidateQueries` em `onSuccess`, em `docuparse-project/frontend/src/modules/documents/hooks/useDocumentMutations.ts`
  - **Resultado (2026-07-24)**: `reprocessDocument`/`deleteDocument`/`validateDocument`, cada um invalidando `documentKeys.all` em `onSuccess` (qualquer `useDocumentsQuery`/`useDocumentCount` ativo refaz a busca sozinho, por prefixo de chave). **Efetivamente conectado**, não só criado: `AppLayout` (`main.tsx`, ainda o shell até a Fase 4i) passou a chamar `reprocessDocument`/`deleteDocument` dentro de `handleReprocessDocument`/`handleDeleteDocument` em vez de `api.post`/`api.delete` diretos — preservando o mesmo tratamento de erro (`readError`) e o `refreshData()` subsequente (ainda necessário para schemas/layouts/`selectedDocument`, não migrados nesta fase). `ValidationView.submitDecision` passou a chamar `validateDocument` em vez de `api.post('/documents/{id}/validate')` direto. `handleSaveFields` (PUT de campos) e a extração LangExtract **não** foram convertidos — fora do escopo literal desta tarefa ("validação/aprovação/rejeição" é só o `POST .../validate`), e ambos têm lógica (polling, versionamento com 409) que não se encaixa no padrão simples de `useMutation` sem trabalho adicional não pedido aqui.
- [X] T030 [P] [US2] Mover `Dashboard`, `InboxView`, `ApprovedView`, `RejectedView`, `ValidationView`, `DocumentTable`, `ExtractedFieldsModal`, `RejectedDocumentModal`, `LangExtractPanel`, `DocumentMetadataPanel`, `FieldVersionHistoryModal` de `src/main.tsx` para `docuparse-project/frontend/src/modules/documents/components/`
  - **Resultado (2026-07-24)**: Todos os 11 componentes movidos; cada extração foi diffada linha a linha contra o `main.tsx` do commit anterior (`git show HEAD:...`) para confirmar que só a fiação de hooks mudou — **um erro de transcrição foi pego e corrigido nesse processo**: o texto placeholder de `ReadOnlyTranscriptionFormatted` ("nenhuma transcrição formatada disponível...") tinha sido digitado de memória incorretamente; o diff contra o original revelou o texto real ("Disponivel apenas para PDFs digitais processados pelo engine Docling.") e foi corrigido antes do commit. **Pré-requisito não listado em nenhuma tarefa**: `DocumentBlobPreview` e `EmailMetadataModal` (usados por `ValidationView`/`DocumentTable` respectivamente) estavam classificados como `shared` em `data-model.md`, mas nenhuma tarefa das Fases 2/4a-4c os movera — extraídos agora para `shared/components/` como consequência necessária (mesmo padrão já registrado em T008/T019: sem isso, `ValidationView`/`DocumentTable` dependeriam de um `main.tsx` cada vez mais vazio). `ChannelMetadata`/`EmailModalDoc` (tipos compartilhados por ambos) foram promovidos de interfaces locais em `main.tsx` para exports de `src/types.ts`. **Limite de 150 linhas/arquivo (FR-012/SC-002)**: `DocumentTable` (215 linhas movida tal-e-qual) foi dividida em `DocumentTable.tsx` (container/sort/estado) + `DocumentTableHead.tsx` + `DocumentTableRow.tsx`. `ValidationView` (367 linhas) exigiu uma decomposição maior — 3 hooks de estado local em `hooks/` (`useFieldExtraction`, `useFieldVersioning`, `useDocumentDecision`, não exportados no barrel do módulo por serem detalhe de implementação de uma única tela) + 4 componentes (`ValidationView` container fino, `ValidationDocumentPreview`, `ValidationDecisionPanel`, `ValidationSaveControls`, `ReadOnlyTranscriptionFormatted`). `data-model.md` já previa "hooks/ # ... + hooks de estado local", então a divisão é consistente com a forma de módulo documentada, não uma invenção. **Achados de acessibilidade corrigidos** (não regressões desta tarefa — o markup migrado é idêntico ao original, que já tinha essas lacunas; corrigidos agora porque T033 os expôs e FR-013/SC-006 pedem conformidade no código migrado): checkbox "selecionar todos" e checkboxes por linha em `DocumentTable` sem rótulo acessível (`title` sozinho não basta para o axe) → `aria-label`; `<select>` de modelo de extração em `LangExtractPanel` sem nome acessível → `aria-label`; `<th>` vazio (coluna de ações) em `DocumentTable` → `<span className="sr-only">`.
- [X] T031 [US2] Expor `docuparse-project/frontend/src/modules/documents/index.ts` (`DocumentsRoutes`, hooks, `documentKeys`) conforme `contracts/module-boundaries.md`
  - **Resultado (2026-07-24)**: Barrel exporta `DocumentsRoutes`, `documentKeys`/`useDocumentsQuery`/`useDocumentCount`/`useDocumentMutations` (+ tipo `ValidateDocumentInput`), e `ApprovedView`/`RejectedView`/`RejectedDocumentModal`/`ValidationView` diretamente (não roteadas por nav real — achado do T012 — ou consumidas fora do router por `AppLayout`/testes, mesmo padrão já usado antes da extração). **Divergência com `contracts/module-boundaries.md`**: o contrato lista `useDocumentQuery` (singular, detalhe de um documento) como parte da API pública — nenhuma tarefa de `tasks.md` (T026-T033) pede a criação desse hook; a busca do documento selecionado continua em `AppLayout`/`main.tsx`, fora do escopo desta sub-fase. Registrado aqui para quem for revisar o contrato antes da Fase 4i (quando `AppLayout` for desmontado).
- [X] T032 [US2] Atualizar `router.tsx` para consumir `DocumentsRoutes`; remover código correspondente de `src/main.tsx`; rodar gate completo + checklist visual; commit da etapa
  - **Resultado (2026-07-24)**: `router.tsx` substitui as 3 rotas inline (`dashboard`/`inbox`/`validation`, com seus componentes de rota locais) por `...DocumentsRoutes` (spread no array de `children`); demais rotas (`upload`/`operations`/`settings`/`users`/`roles`/`tenants`, ainda não extraídas) inalteradas. `main.tsx`: removidos `Dashboard`/`InboxView`/`ApprovedView`/`RejectedView`/`ValidationView`/`DocumentTable`/`ExtractedFieldsModal`/`RejectedDocumentModal`/`LangExtractPanel`/`FieldVersionHistoryModal`/`DocumentMetadataPanel`/`ReadOnlyTranscriptionFormatted`/`DocumentBlobPreview`/`EmailMetadataModal`/`parseFieldEntry`/`pollDocumentExtraction` e os imports/ícones que ficaram órfãos (`useRef`, ícone `X`, `Eye`/`FileJson`/`History`/`Save`/`XCircle`, tipos `ExtractionResult`/`FieldRow`/`SaveMessage`/`ExtractionFieldVersion`/`FieldVersionsResponse`, interface `DashboardMetrics`) — cada remoção verificada por `grep` antes de apagar para não quebrar consumidores ainda não migrados (`ReferenceDocumentPanel`/`DocumentPreview` na Configurações continuam usando `useDocumentPage`/`DocumentBlobPreview`, agora importado do novo local em `shared/components`). `AppLayout` importa `RejectedDocumentModal`/`useDocumentMutations` de `./modules/documents` (barrel) em vez de definição local. **Achado de teste**: `approved.test.tsx`/`rejected.test.tsx`/`validation.test.tsx` renderizavam os componentes com `render()` puro (sem `QueryClientProvider`) — quebraria agora que essas telas usam `useQuery`/`useMutation`. Adicionado `renderWithQueryClient` em `src/__tests__/utils.tsx` (um `QueryClient` novo por render, `retry: false`, evita cache cruzando testes) e os 3 arquivos atualizados para importar de `../modules/documents` em vez de `../main`. **Achado de infraestrutura de teste**: o `queryClient` singleton usado por `renderApp()` (produção) persistia cache entre `it()`s do mesmo arquivo sem nenhum reset — inofensivo enquanto nada usava `useQuery` de verdade, mas um risco real de poluição de cache agora que `Dashboard`/`InboxView`/etc. fazem cache real; adicionado `queryClient.clear()` em `afterEach` de `src/__tests__/setup.ts`. **Gate completo**: `typecheck` limpo; `lint` só com o `max-lines` de `main.tsx` (3603 linhas, decrescente) e os mesmos avisos `exhaustive-deps` pré-existentes documentados desde T004 (mais um novo aviso equivalente em `useFieldExtraction.ts`, o mesmo efeito com o mesmo array de dependências restrito só que agora em arquivo próprio — não é regressão, é o mesmo padrão relocado); `test:run` 39/39 (34 pré-existentes + 5 de T033); `build` OK (mesmo aviso de chunk >500kB documentado em T025). Checklist visual manual **não** executado em navegador (mesma limitação de T011/T018/T020/T025 — sem backends/browser neste ambiente); risco mitigado pelo diff linha-a-linha contra o original feito em T030.
- [X] T033 [P] [US2] Adicionar testes de acessibilidade para `DocumentTable` e `ValidationView` em `docuparse-project/frontend/src/modules/documents/__tests__/`
  - **Resultado (2026-07-24)**: `DocumentTable.a11y.test.tsx` (3 testes: lista, estado vazio, seleção em massa) e `ValidationView.a11y.test.tsx` (2 testes: sem documento selecionado, formulário preenchido). Os testes **encontraram violações reais** (não induzidas por esta tarefa — mesmo markup do monólito original) corrigidas em T030: checkboxes sem rótulo acessível, `<select>` sem nome acessível, `<th>` vazio. `ValidationView` com `DocumentBlobPreview` real dispara uma chamada não mockada (`GET .../file`) que resolve em estado de erro visível (texto acessível) — aceito, mesmo padrão de "erro amigável" já coberto por `flows.test.tsx`.

### 4e. `modules/operations` (DLQ)

- [X] T034 [US2] Criar `docuparse-project/frontend/src/modules/operations/{hooks,components,types.ts,index.ts}` convertendo `OperationsView` e as chamadas de DLQ (summary/events/requeue) para `useQuery`/`useMutation`
  - **Resultado (2026-07-24)**: `types.ts` recebe `DlqStream`/`DlqSummary`/`DlqEvent` tal-e-qual (mesmo índice permissivo, mesma justificativa em comentário). `hooks/queryKeys.ts` define `operationsKeys` (`all → summary()/events(stream)`), análogo a `documentKeys` conforme `data-model.md`. Três hooks: `useDlqSummaryQuery`/`useDlqEventsQuery` (`useQuery`, sem `staleTime` explícito — mesmo padrão já estabelecido por `useDocumentsQuery`/`useDocumentCount` na Fase 4d, que também não seguiu à risca a menção de `data-model.md` a "staleTime explícito"; não introduzido agora por consistência com o precedente real do código, não com a aspiração do documento) e `useRequeueMutation` (`useMutation`; só invalida `operationsKeys.all` quando `execute: true` — `execute: false` é simulação pura, não deve invalidar cache, mesma distinção do código original). `OperationsView` original (220 linhas, `main.tsx:833-1052`) excedia o limite de 150 linhas/componente (FR-012/SC-002) e foi decomposta em container (`OperationsView.tsx`, 102 linhas: estado de `selectedStream`/`selectedEvent`/mensagem de ação, fiação dos 3 hooks) + 3 componentes de apresentação (`DlqStreamSummary`, `DlqEventsTable`, `DlqEventDetail`, 53-68 linhas cada), mesmo padrão de decomposição já usado em `DocumentTable`/`ValidationView` (T030). **Verificação de fidelidade**: diff textual (strings/classNames/endpoints) entre o bloco original e os arquivos novos confirmou nenhuma divergência de conteúdo além da reorganização estrutural esperada (imports/exports) — nenhum texto, endpoint ou classe Tailwind alterado. Única mudança de comportamento *intencional*: a mensagem de erro de carregamento (antes só possível via `catch` do `Promise.all` dentro do `message` local) agora deriva do `error` de cada `useQuery` (`bannerMessage`/`bannerTone` dão prioridade à mensagem de ação — resultado de simular/reenfileirar — e caem para o erro de carga só na ausência dela), preservando a única região de `Alert` visível já existente; `loading` usa `isFetching` (não `isLoading`) para continuar exibindo "Carregando operacoes..." em todo refresh/troca de stream, não só no primeiro fetch, replicando o `setLoading(true)` do `loadOperations` original que rodava em toda chamada. `selectedEvent` é limpo de imediato ao trocar de stream (no clique), em vez de só após o load resolver — mesmo resultado final, sem espera perceptível.
- [X] T035 [US2] Atualizar `router.tsx` para consumir `OperationsRoutes`; remover código de `src/main.tsx`; rodar gate completo + checklist visual; commit da etapa
  - **Resultado (2026-07-24)**: `router.tsx` substitui a rota inline `operations` (com `OperationsRoute` local importando `OperationsView` de `../main`) por `...OperationsRoutes` (spread no array de `children`, mesmo padrão já usado para `DocumentsRoutes` em T032). `main.tsx`: removidos `OperationsView`, `DEFAULT_DLQ_STREAM`, `DlqStream`/`DlqSummary`/`DlqEvent` (linhas 804-1052 do estado pré-tarefa); import de `formatDate`/`Metric`/`KeyValueGrid` de `shared/components`/`shared/utils` removido do topo de `main.tsx` por ficarem órfãos (únicos consumidores eram o bloco removido; `RefreshCw`/`AlertTriangle`/`FileText` permanecem — usados por `AppLayout`/`NAV_ITEMS`/`UploadView`/outras telas ainda não extraídas). **Gate completo**: `typecheck` limpo; `lint` só com o `max-lines` esperado em `main.tsx` (3582 linhas, decrescente) + os mesmos 10 avisos `exhaustive-deps` pré-existentes documentados desde T004/T032 — nenhum problema novo, nenhuma violação de fronteira de módulo (import sempre via barrel `../modules/operations`); `test:run` 39/39 (11 arquivos), incluindo o teste de integração "Operações (DLQ)" já existente em `flows.test.tsx` (navega pela árvore real, exercitando `OperationsRoutes`/`OperationsView`/os 3 hooks novos de ponta a ponta via MSW, sem alteração de asserções); `build` OK (mesmo aviso de chunk >500kB documentado desde T025). Checklist visual manual (`quickstart.md`) **não** executado em navegador nesta etapa — mesma limitação de ambiente já registrada em T011/T018/T020/T025/T032 (sem backends/browser neste ambiente); risco mitigado pelo diff textual linha-a-linha contra o original (ver T034) e pelo teste de integração via MSW cobrindo o fluxo completo (listar streams → selecionar evento → simular → reenfileirar).

### 4f. `modules/settings` (+ React Hook Form + Zod)

- [X] T036 [P] [US2] Criar schemas Zod espelhando as validações manuais atuais de cada aba (OCR/Email/WhatsApp/Integrações) em `docuparse-project/frontend/src/modules/settings/schemas/`
  - **Resultado (2026-07-24)**: 3 schemas criados (`ocrSettingsSchema.ts`, `emailSettingsSchema.ts`, `integrationSettingsSchema.ts`), cada um com o `z.object` + tipo `*FormValues` inferido + `*_DEFAULTS` (valores default idênticos aos `useState` originais de `main.tsx`). **WhatsApp não ganhou schema** (decisão registrada na própria tarefa): `WhatsAppSettingsPanel` (main.tsx ~2363-2427) é 100% estático — todo input é não-controlado/`defaultValue`/`placeholder`, sem nenhum backend por trás; só o botão "Processar arquivos do WhatsApp" faz algo real (`POST /whatsapp/poll`, sem payload de formulário). Inventar um schema para campos que não são persistidos por nada violaria "espelhar validações atuais" (não há validação atual nenhuma) — ver T037 para o tratamento dado ao painel. **Desvio de fidelidade**: os campos numéricos (`timeout_seconds`/`digital_pdf_min_text_blocks`/`imap_port`/`max_attachment_mb`) usam `z.number().min().max()` espelhando os atributos `min`/`max` HTML dos inputs originais — que hoje só são validação nativa do navegador, nunca bloqueada em JS antes do POST. O `zodResolver` passa a bloquear de fato o submit fora da faixa; é uma pequena melhoria de comportamento (os limites já existiam no markup, só não eram garantidos), não uma regressão, e documentado no comentário de cada schema. **Achado técnico**: a combinação zod v4 (`^4.4.3`) + `@hookform/resolvers` v5 quebra o typecheck se os campos numéricos usarem `z.coerce.number()` (o par `input`/`output` diferente do coerce gera um `UseFormRegister` incompatível entre o schema e o `useForm<T>` inferido a partir de `defaultValues`) — resolvido usando `z.number()` puro + `register(campo, { valueAsNumber: true })` no lado do input, evitando o split de tipos por completo.
- [X] T037 [US2] Dividir `SettingsView` original em subcomponentes ≤150 linhas por aba (`OcrSettingsPanel`, `EmailSettingsPanel`, `WhatsAppSettingsPanel`, `IntegrationSettingsPanel`) em `docuparse-project/frontend/src/modules/settings/components/`, cada um usando `useForm` + `zodResolver`
  - **Resultado (2026-07-24)**: `OcrSettingsPanel`/`EmailSettingsPanel`/`IntegrationSettingsPanel` viraram self-contained (decisão do handoff): cada um faz seu próprio GET-on-mount (`useEffect`+`api.get`, preservando `reset({...DEFAULTS, ...response.data})` — o "merge sobre defaults" do código original, essencial porque os handlers MSW de teste devolvem `{}`), seu `useForm`+`zodResolver`, seu PATCH de submit, e sua própria `Alert` de sucesso/erro local (antes um único `message` compartilhado por toda `SettingsView`, agora uma mensagem por painel — mesma categoria de relocação de UI já documentada por T034 para `bannerMessage`). Cada um foi dividido em container + subcomponentes de campos para caber em ≤150 linhas: `OcrSettingsPanel` (88L) + `OcrRoutingSummary` (53L) + `OcrSettingsFields` (108L); `EmailSettingsPanel` (129L) + `EmailAccountFields` (69L) + `EmailAttachmentRules` (37L); `IntegrationSettingsPanel` ficou em um único arquivo (121L, RHF não empurrou muito além do tamanho original). Selects booleanos (`retry_empty_text_enabled`, `is_active`, `approved_export_enabled`) usam `Controller` (não `register` puro) porque o valor do formulário é `boolean` mas as `<option>` são strings `'enabled'`/`'disabled'` — `register` uncontrolled não sincronizaria a opção selecionada a partir de um `boolean`. **WhatsApp movido tal-e-qual, sem `useForm`** (ver T036) — continua recebendo `onPoll` por prop, exatamente como antes; ganhou uma prop opcional `message` só para exibição (o fetch/estado de `pollWhatsApp`, que antes vivia no `message` compartilhado de `SettingsView`, agora está em `useWhatsAppPoll` — chamado pelo container `SettingsView`, não pelo painel). **Decisão #4 (email condicional) preservada**: "Testar captura IMAP" mantém o guard imperativo original (host/usuário obrigatórios só quando `provider === 'imap'`, só para o botão de teste) fora do schema Zod — o handler `onPoll` primeiro roda o guard, depois chama o mesmo `handleSubmit` da validação real (então um `imap_port` fora da faixa também bloqueia o teste, igual ao save), só então faz o POST de poll. "Salvar email" continua tão permissivo quanto antes (aceita host/usuário vazios).
- [X] T038 [P] [US2] Mover `SchemaFieldsEditor`, `ExamplesEditor`, `ReferenceDocumentPanel`, `SchemaList`, `DeleteSchemaModal`, `ConfigList` para `docuparse-project/frontend/src/modules/settings/components/`
  - **Resultado (2026-07-24)**: Os 6 componentes movidos, `SchemaFieldsEditor`/`ExamplesEditor` tal-e-qual (mesmo JSX/classes). `DocumentPreview`/`HighlightedOcrText` (consumidos só por `ReferenceDocumentPanel`/`ExtractionTestTab`, nunca listados em nenhuma tarefa como "compartilhados") também vieram para cá, não para `shared/components` — são específicos da área de Extração de Configurações, mesmo critério já usado por T030 para decidir o que é módulo-específico vs. `shared`. **`ReferenceDocumentPanel` trocou `useDocumentPage` (morto, ver T039) por `useDocumentsQuery` do módulo `documents`** (`import { useDocumentsQuery } from '../../documents'`), decisão #6 do handoff — mesma dependência cruzada legítima documentada em `contracts/module-boundaries.md`. **`DeleteSchemaModal`/`SchemaList` convertidos para `useSchemaMutations().deleteSchema`** (`useMutation`, invalida `settingsKeys.all`) em vez de `api.delete` direto + callback `onDeleted` propagando `onChanged()`/`refreshData()` do pai — consequência necessária de `schemas` não vir mais de `AppOutletContext` (ver T039); `SchemaList` perdeu a prop `onDeleted` (só recebe `schemas` agora, a lista se atualiza sozinha via invalidação de query), `DeleteSchemaModal` perdeu `onDeleted` em favor de só `onClose` (chamado após sucesso). Estado de loading (`deleting`, do `isPending` da mutation) preservado para manter o botão "Excluindo..." desabilitado igual ao original.
- [X] T039 [US2] Converter busca de schemas/layouts para `useQuery` (`settingsKeys`, análogo a `documentKeys`); expor `docuparse-project/frontend/src/modules/settings/index.ts`; atualizar `router.tsx`; remover código de `src/main.tsx`; rodar gate completo + checklist visual; commit da etapa
  - **Resultado (2026-07-24)**: `settingsKeys` (`all → schemas()/layouts()`) + `useSchemasQuery`/`useLayoutsQuery` criados (`GET /schema-configs`/`GET /layout-configs`, mesmo padrão de `documentKeys`/`useDocumentsQuery`). **`schemas`/`layouts` removidos de `AppOutletContext`** (decisão #5 do handoff): `AppLayout` (`main.tsx`) parou de buscá-los em `refreshData` (só continua buscando o documento selecionado, usado por Validação); `SettingsView` chama `useSchemasQuery`/`useLayoutsQuery` diretamente; `ValidationRoute` (`modules/documents/routes/ValidationRoute.tsx`) passou a importar `useSchemasQuery` do barrel `../../settings` em vez de ler `schemas` do outlet context — exatamente o caso de dependência cruzada legítima que `contracts/module-boundaries.md` já documentava para essa direção ("settings ... Consumido por: modules/documents"). Grep confirmou que `layouts` não tem nenhum consumidor fora de `modules/settings`, então só `useSchemasQuery` foi exportado para consumo externo (via barrel), `useLayoutsQuery` fica só de uso interno do módulo (ainda exportado no barrel por consistência com o contrato, mas sem consumidor cross-module hoje). **`createSchema`/`saveDraft`/`createLayout` viraram `useMutation`** (`useSchemaMutations`/`useLayoutMutations`, invalidando `settingsKeys.all` em `onSuccess`) substituindo o antigo `onChanged()` prop-callback — consequência necessária, mesma categoria já registrada em T008/T019/T030/T034. `router.tsx`: rota `settings` inline (que importava `SettingsView` de `../main`) substituída por `...SettingsRoutes` (spread), mesmo padrão de `DocumentsRoutes`/`OperationsRoutes`; guarda `PermissionGuard code="roles.manage"` preservada. **`main.tsx`**: removidos ~2.700 linhas — `SettingsView` e todos os seus 27 helpers privados (`SETTINGS_TABS`/`SETTINGS_AREAS`/`SETTINGS_TAB_HELP`/`PROMPT_HINTS`, as 6 interfaces de formulário, `TabHelp`/`OcrSettingsPanel`/`EngineSelect`/`engineLabel`/`EmailSettingsPanel`/`WhatsAppSettingsPanel`/`IntegrationSettingsPanel`/`ConfigIntro`/`ActiveTemplateHeader`/`SettingsStepActions`/`HintPanel`/`ReferenceDocumentPanel`/`DocumentPreview`/`HighlightedOcrText`/`SchemaFieldsEditor`/`ExamplesEditor`/`PROTECTED_SCHEMA_IDS`/`DeleteSchemaModal`/`SchemaList`/`ConfigList`/os 6 helpers `buildLangExtract*`), verificado por grep antes de cada remoção (mesmo processo de T032). **`useDocumentPage`/`PAGE_SIZE`/`EMPTY_PAGE` também removidos**: único consumidor restante era o `ReferenceDocumentPanel` antigo, já movido/convertido para `useDocumentsQuery` (T038) — grep confirmou zero consumidores remanescentes. Removidos também os imports órfãos do topo (`useMemo` — não, permanece usado; especificamente: os 4 blocos `./models/boleto|nota_fiscal|contadeagua|recibo/*`, `SearchInput`, `DocumentBlobPreview`, `SchemaConfig`, `LayoutConfig`, `SchemaField`, `SchemaExample`, `Paginated`, `DocumentListParams`, ícones `Trash2`/`CheckCircle2`) — cada remoção confirmada por grep (`Check`/`Copy`/`ChevronDown`/`Building2`/`ClipboardCheck`/`Settings`/`RefreshCw` continuam em uso por `TenantsView`/`NAV_ITEMS`/`AppLayout`, mantidos). `main.tsx` caiu de 3581 para 1075 linhas. **Gate completo**: `typecheck` limpo; `lint` 1 erro (`max-lines` em `main.tsx`, esperado até T046) + 7 avisos `exhaustive-deps` — todos pré-existentes relocados (3 em `usePromptSyncEffects.ts`, 1 em `useReferenceDocumentLoader.ts`, 1 em `useAutoClassification.ts`, mais os 2 que já existiam fora da área de Configurações desde antes: `main.tsx:525` e `useFieldExtraction.ts`) — nenhuma categoria nova; `test:run` 39/39 (11 arquivos, antes de T040 adicionar os testes de a11y); `build` OK (mesmo aviso de chunk >500kB documentado desde T025). Checklist visual manual **não** executado em navegador — mesma limitação de ambiente de todas as tarefas de integração anteriores (T011/T018/T020/T025/T032/T035): sem backends/browser neste sandbox; risco mitigado por diff textual contra o `main.tsx` original ao mover cada bloco e pela suíte MSW cobrindo o fluxo completo de navegação/salvamento em `flows.test.tsx`.
- [X] T040 [P] [US2] Adicionar testes de acessibilidade para os painéis de Configurações migrados em `docuparse-project/frontend/src/modules/settings/__tests__/`
  - **Resultado (2026-07-24)**: 4 arquivos (`OcrSettingsPanel.a11y.test.tsx`, `EmailSettingsPanel.a11y.test.tsx`, `IntegrationSettingsPanel.a11y.test.tsx`, `WhatsAppSettingsPanel.a11y.test.tsx`), mais um helper `renderSettingsPanel.tsx` (não é `*.test.tsx`, mas cai sob o override `**/__tests__/**` do `max-lines`, mesmo tratamento de `src/__tests__/utils.tsx`). **Desvio do padrão de `DocumentTable.a11y.test.tsx`**: os 3 painéis com `useForm` também chamam `useAuth()` (para `currentTenant`) e fazem `useQuery`-like GET-on-mount, então `renderWithQueryClient` (só `QueryClientProvider`) não bastava — `renderSettingsPanel` combina `QueryClientProvider` (novo `QueryClient` por render, `retry:false`) + `AuthProvider` (mesmo padrão de `LoginPage.a11y.test.tsx`). Cada teste registra um handler MSW (`server.use(http.get('/api/ocr/settings/...', () => HttpResponse.json({})))`) para o GET de mount, evitando aviso de "unhandled request" e garantindo o formulário renderizado com os `*_DEFAULTS` antes do `axe()`. **Nenhuma violação de acessibilidade real encontrada** nos painéis migrados (diferente de T033, que achou e corrigiu 3 problemas em `DocumentTable`/`ValidationView`) — o markup usa `Field` (que já envolve `label`+`input` em um `<label>`, associação implícita) para todo input, então a extração não introduziu nem herdou lacunas de rótulo. `WhatsAppSettingsPanel` recebeu um teste smoke (`render` direto, sem `AuthProvider`/`QueryClientProvider` — não precisa, é puramente apresentacional). Suíte final: 43/43 testes (15 arquivos, +4 sobre os 39/11 de T039).

### 4g. `modules/admin`

- [ ] T041 [US2] Criar `docuparse-project/frontend/src/modules/admin/` movendo `GerenciarUsuarios`/`GerenciarRoles` de `src/main.tsx`, convertendo fetch/mutations para TanStack Query; expor barrel; atualizar `router.tsx`; rodar gate completo + checklist visual; commit da etapa

### 4h. `modules/upload`

- [ ] T042 [US2] Criar `docuparse-project/frontend/src/modules/upload/` movendo `UploadView` de `src/main.tsx`; expor barrel; atualizar `router.tsx`; rodar gate completo + checklist visual; commit da etapa

### 4i. Zustand residual, formulários restantes e remoção final do monólito

- [ ] T043 [US2] Revisar o estado remanescente após T016–T042; criar slice(s) Zustand apenas para estado de cliente genuíno identificado (`docuparse-project/frontend/src/shared/store/` ou dentro do módulo específico) — ver `research.md` §5; pular esta tarefa se nada restar
- [ ] T044 [P] [US2] Converter `LoginPage` (login/registro) para `react-hook-form` + `zodResolver` em `docuparse-project/frontend/src/modules/auth/components/LoginPage.tsx`, preservando mensagens/regras de validação atuais (FR-005)
- [ ] T045 [US2] Formalizar `docuparse-project/frontend/src/shared/utils/errorMessages.ts` a partir de `readError`/`asApiError`; aplicar em todos os error boundaries e telas migradas (FR-007)
- [ ] T046 [US2] Remover `docuparse-project/frontend/src/main.tsx` (monólito original, já vazio após T018–T045); confirmar `docuparse-project/frontend/src/app/main.tsx` (T024) como único bootstrap; rodar gate completo + checklist visual; commit da etapa

**Checkpoint**: User Story 2 completa — arquitetura-alvo integralmente implantada, `src/main.tsx` não existe mais.

---

## Phase 5: User Story 3 - Rede de segurança que impede reintrodução de anti-padrões (Priority: P2)

**Goal**: Ativar e comprovar que o lint bloqueia os anti-padrões identificados, incluindo a fronteira de módulo (que só é totalmente significativa com os módulos da Fase 4 já existindo).

**Independent Test**: Introduzir deliberadamente uma violação de cada regra-chave e confirmar que `npm run lint` reporta erro.

### Implementation for User Story 3

- [ ] T047 [US3] Habilitar a regra de fronteira de módulo (`eslint-plugin-boundaries`, configurada em `docuparse-project/frontend/eslint.config.js`) apontando para `src/modules/*` criados na Fase 4; rodar `npm run lint` e confirmar zero violações reais no código migrado
- [ ] T048 [P] [US3] Introduzir deliberadamente um import direto de arquivo interno de outro módulo (fora do barrel) e confirmar que `npm run lint` falha; reverter a alteração sem commitá-la
- [ ] T049 [P] [US3] Introduzir deliberadamente um `any` não justificado e confirmar que `npm run lint` falha; reverter a alteração sem commitá-la
- [ ] T050 [US3] Adicionar `npm run lint`, `npm run typecheck` e `npm run test:run` como gate obrigatório no pipeline de CI do frontend (respeitando o caminho frontend-only já existente desde o commit `a6c2235`)

**Checkpoint**: Rede de segurança verificada e conectada ao CI — anti-padrões bloqueados automaticamente antes do merge.

---

## Phase 6: User Story 4 - Evolução incremental, sempre entregável (Priority: P3)

**Goal**: Confirmar, ao final, que toda a transição ocorreu em etapas pequenas e entregáveis, sem exigir indisponibilidade nem deixar estado quebrado em nenhum ponto intermediário.

**Independent Test**: Revisar o histórico de commits da branch `016-frontend-architecture-refactor` e confirmar que cada etapa de extração teve commit próprio com gate completo verde.

### Implementation for User Story 4

- [ ] T051 [US4] Revisar o histórico de commits desta branch e confirmar que cada etapa das Fases 2–5 (T010, T011, T018, T020, T025, T032, T035, T039, T041, T042, T046, T047) foi um commit isolado com gate completo verde, sem nenhum commit "big-bang"
- [ ] T052 [US4] Documentar no(s) PR(s) (ver `research.md` §11) a sequência de entrega incremental realizada, referenciando os checkpoints de cada sub-fase

**Checkpoint**: Todas as user stories concluídas e verificadas independentemente.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Fechamento — itens que afetam a feature como um todo, não uma story específica.

- [ ] T053 [P] Atualizar `docs/specs/016-frontend-architecture-refactor/quickstart.md` com qualquer ajuste descoberto durante a execução das Fases 2–6
- [ ] T054 Reavaliar os escapes `[key: string]: any`/`unknown` em `docuparse-project/frontend/src/shared/types/` e nos tipos `DlqStream`/`DlqEvent`, apertando o que for estável (mantendo índice permissivo só onde o payload é legitimamente heterogêneo)
- [ ] T055 Rodar `npm run build` de produção final em `docuparse-project/frontend` e validar o bundle/preview local
- [ ] T056 Executar o checklist de regressão visual completo (`quickstart.md`) uma última vez, ponta a ponta, sobre a estrutura final

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: sem dependências — pode começar imediatamente
- **Foundational (Phase 2)**: depende do Setup — **bloqueia** todas as user stories
- **User Story 1 (Phase 3)**: depende apenas do Foundational; pode ser entregue isoladamente (MVP mínimo: rede de regressão reforçada, sem nenhuma extração estrutural)
- **User Story 2 (Phase 4)**: depende do Foundational; suas sub-fases internas (4a→4i) são **sequenciais** entre si (não paralelizáveis como um todo — `documents` depende de `auth`+roteamento já extraídos, `settings` referencia `documents`, etc. — ver `research.md` §2)
- **User Story 3 (Phase 5)**: depende da Phase 4 estar completa (a regra de fronteira de módulo só é significativa com os módulos já existindo); a configuração *básica* de lint (T004) já roda desde a Phase 2
- **User Story 4 (Phase 6)**: depende de todas as fases anteriores estarem concluídas (é uma verificação retrospectiva do processo)
- **Polish (Phase 7)**: depende de todas as user stories desejadas estarem completas

### Within Phase 4 (User Story 2)

- 4a (`shared/components`) → 4b (`auth`) → 4c (roteamento) → 4d (`documents`) → 4e (`operations`) → 4f (`settings`) → 4g (`admin`) → 4h (`upload`) → 4i (Zustand/RHF-Zod restante/limpeza final)
- Dentro de cada sub-fase, tarefas marcadas `[P]` (ex. T016/T017, T028/T030, T036/T038) tocam arquivos diferentes e podem ser feitas em paralelo; a tarefa de integração final de cada sub-fase (ex. T018, T032, T039) depende de todas as `[P]` daquela sub-fase estarem prontas.

### Parallel Opportunities

- Setup: T002 e T003 em paralelo
- Foundational: T005, T006, T009 em paralelo (após T004/T007/T008)
- User Story 1: T012 antes de T013; T013/T014 podem ocorrer em paralelo depois
- User Story 2, por sub-fase: ver marcações `[P]` acima (T016/T017; T028/T030; T036/T038)
- User Story 3: T048 e T049 em paralelo (violações independentes)

---

## Parallel Example: Sub-fase 4a (`shared/components`)

```bash
# Extração das primitivas de UI pode ocorrer em paralelo (arquivos diferentes):
Task: "Extrair Alert, EmptyState, Field, Metric para src/shared/components/"
Task: "Extrair SearchInput, StatusBadge, KeyValueGrid, Pagination, ConfirmDialog para src/shared/components/"
```

---

## Implementation Strategy

### MVP First (User Story 1 apenas)

1. Completar Phase 1: Setup
2. Completar Phase 2: Foundational (crítico — bloqueia tudo)
3. Completar Phase 3: User Story 1
4. **PARAR e VALIDAR**: rede de regressão reforçada já é um incremento de valor entregável isoladamente, mesmo sem nenhuma extração estrutural

### Incremental Delivery (recomendado)

1. Setup + Foundational → fundação pronta
2. User Story 1 → rede de regressão reforçada → entregar/demo
3. User Story 2, sub-fase por sub-fase (4a→4i) → cada uma entregável isoladamente (commit + gate verde), conforme US4
4. User Story 3 → lint de fronteira de módulo ativo e verificado no CI
5. User Story 4 → fechamento/documentação do processo incremental
6. Polish → limpeza final

### Notes

- `[P]` = arquivos diferentes, sem dependência entre si
- Rodar o gate completo (`npm run typecheck && npm run lint && npm run test:run && npm run build`, ver `quickstart.md`) ao final de cada tarefa de integração marcada acima — não avançar com gate vermelho
- Commit por etapa/sub-fase concluída, nunca por "toda a Phase 4 de uma vez"
- Evitar: mover e reescrever ao mesmo tempo — mover primeiro preservando comportamento, só então melhorar
