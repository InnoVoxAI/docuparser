# Development dependencies for TrOCR engine support
# Install with: pip install -r requirements-dev.txt

# TrOCR engine dependencies (Transformer-based OCR for handwritten documents)
# Required only if using the TrOCR engine for handwritten document processing
transformers==4.45.2
torch==2.4.1
torchvision==0.19.1

# Additional development tools
pytest==8.3.3
pytest-cov==6.0.0
moto[s3]>=5.0  # mock S3 para testes de storage (sem rede/disco)
